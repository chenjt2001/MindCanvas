<Page
    x:Class="MindCanvas.DefaultSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MindCanvas"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource SystemControlAcrylicWindowBrush}">
    <Page.Transitions>
        <TransitionCollection>
            <EdgeUIThemeTransition Edge="Top">
            </EdgeUIThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <local:AppTitleBarControl Grid.Row="0"/>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button x:Name="BackButton"
                Style="{StaticResource NavigationBackButtonNormalStyle}"
                IsEnabled="{x:Bind Frame.CanGoBack, Mode=OneWay}" 
                ToolTipService.ToolTip="后退"
                Click="BackButton_Click"/>
            
            
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" >
                    <TextBlock HorizontalAlignment="Center"  Margin="0,4,0,0" Style="{StaticResource HeaderTextBlockStyle}" Text="默认值设置"/>

                    <TextBlock Style="{StaticResource TitleTextBlockStyle}" Margin="0,40,0,0" FontWeight="Normal" Text="点的默认边框颜色" />
                        <ColorPicker
                            Margin="0,10,0,0"
                            x:Name="BorderBrushColorPicker"
                            ColorSpectrumShape="Box"
                            IsMoreButtonVisible="True"
                            IsColorSliderVisible="True"
                            IsColorChannelTextInputVisible="True"
                            IsHexInputVisible="True"
                            IsAlphaEnabled="False"
                            IsAlphaSliderVisible="True"
                            IsAlphaTextInputVisible="True"/>

                    <TextBlock Style="{StaticResource TitleTextBlockStyle}" Margin="0,40,0,0" FontWeight="Normal" Text="点的默认文字大小" />
                    <muxc:NumberBox 
                        Margin="0,10,0,0"
                        x:Name="FontSizeNumberBox" 
                        Header="设置字号：" 
                        PlaceholderText="1-100的数" 
                        Minimum="10"
                        Maximum="100"
                        SpinButtonPlacementMode="Inline" 
                        ValueChanged="FontSizeNumberBox_ValueChanged"/>

                    <MenuFlyoutSeparator Margin="0,20,0,0"/>
                    <Button x:Name="InitializeBtn" Margin="0,20,0,0" HorizontalAlignment="Right" Content="恢复初始值" FontSize="15" Width="150" Click="InitializeBtn_Click"/>

                    <Button x:Name="CancelBtn" Margin="0,40,0,0" HorizontalAlignment="Right" Content="取消" FontSize="15" Width="100" Click="CancelBtn_Click"/>
                    <Button x:Name="SaveBtn" Margin="0,10,0,0" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Right" Content="保存" FontSize="15" Width="100" Click="SaveBtn_Click"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>

    </Grid>
</Page>
