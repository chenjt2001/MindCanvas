<UserControl
    x:Class="MindCanvas.LoadingControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MindCanvas"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <!--
        <UserControl.Transitions>
            <TransitionCollection>
                <ReorderThemeTransition/>
            </TransitionCollection>
        </UserControl.Transitions>
    -->

    <Grid x:Name="LoadingGrid">
        <Grid.Background>
            <SolidColorBrush Color="Black" Opacity="0.5"/>
        </Grid.Background>
        
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">

            <ProgressRing Name="LoadingProgressRing" Width="100" Height="100" Foreground="White"/>

            <TextBlock x:Name="LoadingTextBlock" Foreground="White" FontSize="24" Text="Loading..."/>

        </StackPanel>

    </Grid>
</UserControl>
