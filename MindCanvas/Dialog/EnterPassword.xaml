<ContentDialog
    x:Class="MindCanvas.Dialog.EnterPassword"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MindCanvas.Dialog"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Uid="EnterPassword">
    <ContentDialog.Resources>
        <Style TargetType="local:EnterPassword" BasedOn="{StaticResource DefaultContentDialogStyle}"/>
    </ContentDialog.Resources>

    <Grid>
        <StackPanel HorizontalAlignment="Left">
            <PasswordBox Visibility="Collapsed" x:Name="PasswordBox0" Width="300" PasswordChanged="PasswordBox_PasswordChanged"/>
            <PasswordBox Visibility="Collapsed" x:Name="PasswordBox1" Width="300" PasswordChanged="PasswordBox_PasswordChanged"/>
            <PasswordBox Visibility="Collapsed" x:Name="PasswordBox2" Width="300" PasswordChanged="PasswordBox_PasswordChanged"/>
        </StackPanel>
    </Grid>
    
</ContentDialog>
