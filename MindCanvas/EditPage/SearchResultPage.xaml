<Page
    x:Class="MindCanvas.EditPage.SearchResultPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MindCanvas.EditPage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource SystemControlAcrylicWindowBrush}">

    <Grid>
        <ScrollViewer
                Padding="0,0,0,0"
                VerticalScrollBarVisibility="Auto"
                VerticalScrollMode="Auto">

            <StackPanel Orientation="Vertical" Margin="20,40,20,0">
                <StackPanel.ChildrenTransitions>
                    <TransitionCollection>
                        <ContentThemeTransition />
                    </TransitionCollection>
                </StackPanel.ChildrenTransitions>

                <TextBlock x:Uid="SearchResultPage_SearchResults" Style="{StaticResource TitleTextBlockStyle}"/>

                <TextBlock x:Name="NoResultsFoundTextBlock" x:Uid="SearchResultPage_NoResultsFound" Visibility="Collapsed" Margin="0,20,0,0"/>
                <ListView x:Name="SearchResultListView" IsItemClickEnabled="True" ItemClick="SearchResultListView_ItemClick" Visibility="Collapsed" Margin="0,20,0,0"/>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
