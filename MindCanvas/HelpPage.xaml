<Page
    x:Class="MindCanvas.HelpPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MindCanvas"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource SystemControlAcrylicWindowBrush}">

    <Grid>
        <ScrollViewer
            Padding="20,0,0,0"
            VerticalScrollBarVisibility="Auto"
            VerticalScrollMode="Auto">
            <StackPanel HorizontalAlignment="Left">

                <TextBlock
                    Style="{StaticResource TitleTextBlockStyle}"
                    Margin="0,4,0,0"
                    FontWeight="Normal"
                    Text="介绍" />

                <RichTextBlock MaxWidth="840" Margin="0,10,0,0" IsTextSelectionEnabled="True">
                    <Paragraph>
                        MindCanvas 是一款制作思维导图的软件。使用 MindCanvas，你可以通过添加点、线和墨迹轻松创造出自己的思维导图，并导出或分享。
                    </Paragraph>
                </RichTextBlock>
                
                <TextBlock
                    Style="{StaticResource TitleTextBlockStyle}"
                    Margin="0,40,0,0"
                    FontWeight="Normal"
                    Text="新增内容" />
                <Button x:Name="NewFunctionBtn" Content="版本新功能" Margin="0,10,0,0" Click="NewFunctionBtn_Click"/>

                <TextBlock
                    Style="{StaticResource TitleTextBlockStyle}"
                    Margin="0,40,0,0"
                    FontWeight="Normal"
                    Text="反馈" />
                <RichTextBlock MaxWidth="840" Margin="0,10,0,0" IsTextSelectionEnabled="True">
                    <Paragraph>
                        您的评价对 MindCanvas 的发展至关重要，请
                        <Hyperlink NavigateUri="ms-windows-store://review/?ProductId=9MZ0K1FKVZP1">为 MindCanvas 评分</Hyperlink>
                        。
                    </Paragraph>
                </RichTextBlock>

                <TextBlock
                    Style="{StaticResource TitleTextBlockStyle}"
                    Margin="0,40,0,0"
                    FontWeight="Normal"
                    Text="关于" />

                <RichTextBlock MaxWidth="840" Margin="0,10,0,0" IsTextSelectionEnabled="True">
                    <Paragraph>
                        作者：
                        珒陶（陈锦涛）
                    </Paragraph>
                    <Paragraph Margin="0,10,0,0">
                        版本：
                        <Run Text="{x:Bind Version}"/>
                    </Paragraph>
                    <Paragraph Margin="0,10,0,0">
                        Copyright © 2021 珒陶. All rights reserved.
                    </Paragraph>
                    <Paragraph Margin="0,10,0,0">
                        更多信息可访问
                        <Hyperlink NavigateUri="http://www.chenjt.com/mindcanvas">官方网站</Hyperlink>
                        及
                        <Hyperlink NavigateUri="http://www.chenjt.com/mindcanvas-privacy">隐私策略</Hyperlink>
                        。
                    </Paragraph>
                </RichTextBlock>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
